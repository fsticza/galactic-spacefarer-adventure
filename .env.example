# Cosmic notification transport (see srv/notification-service.js)
#
# Default (nothing set): mails are rendered by nodemailer's JSON transport and logged, never sent.
#
# Real SMTP delivery (also selected automatically when SMTP_URL is set):
#MAIL_TRANSPORT=smtp
#SMTP_URL=smtps://user:password@smtp.example.com:465
