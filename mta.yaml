_schema-version: 3.3.0
ID: galactic-spacefarer-adventure
version: 1.0.0
description: "Galactic Spacefarer Adventure: SAP CAP service and Fiori Elements app (SAP BTP full-stack take-home)"
parameters:
  enable-parallel-deployments: true
build-parameters:
  before-all:
    - builder: custom
      commands:
        - npm ci
        - npx cds build --production
modules:
  - name: galactic-spacefarer-adventure-srv
    type: nodejs
    path: gen/srv
    parameters:
      instances: 1
      buildpack: nodejs_buildpack
    build-parameters:
      builder: npm-ci
    provides:
      - name: srv-api # required by consumers of CAP services (e.g. approuter)
        properties:
          srv-url: ${default-url}
    requires:
      - name: galactic-spacefarer-adventure-auth
      - name: galactic-spacefarer-adventure-db

  - name: galactic-spacefarer-adventure-db-deployer
    type: hdb
    path: gen/db
    parameters:
      buildpack: nodejs_buildpack
    requires:
      - name: galactic-spacefarer-adventure-db
  - name: galactic-spacefarer-adventure
    type: approuter.nodejs
    path: .deploy/app-router
    parameters:
      keep-existing-routes: true
      disk-quota: 256M
      memory: 256M
    requires:
      - name: srv-api
        group: destinations
        properties:
          name: srv-api # must be used in xs-app.json as well
          url: ~{srv-url}
          forwardAuthToken: true
      - name: galactic-spacefarer-adventure-auth
    provides:
      - name: app-api
        properties:
          app-protocol: ${protocol}
          app-uri: ${default-uri}

resources:
  - name: galactic-spacefarer-adventure-auth
    type: org.cloudfoundry.managed-service
    parameters:
      service: xsuaa
      service-plan: application
      path: ./xs-security.json
      config:
        xsappname: galactic-spacefarer-adventure-${org}-${space}
        tenant-mode: dedicated
        oauth2-configuration:
          credential-types:
            - "binding-secret"
            - "x509"
          redirect-uris:
            - https://*~{app-api/app-uri}/**
        role-collections:
          - name: 'SpacefarerViewer (galactic-spacefarer-adventure ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.SpacefarerViewer'
          - name: 'SpacefarerManager (galactic-spacefarer-adventure ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.SpacefarerManager'
          - name: 'GalacticAdmin (galactic-spacefarer-adventure ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.GalacticAdmin'
          - name: 'userattributes (galactic-spacefarer-adventure ${org}-${space})'
            description: 'generated'
            role-template-references:
              - '$XSAPPNAME.userattributes'
    requires:
      - name: app-api
  - name: galactic-spacefarer-adventure-db
    type: com.sap.xs.hdi-container
    parameters:
      service: hana
      service-plan: hdi-shared
