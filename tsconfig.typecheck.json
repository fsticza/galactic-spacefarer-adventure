{
  "//": "Type-checks the sources plus the tests. tsconfig.json itself stays narrow, because cds build compiles everything it includes into gen/srv.",
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "noEmit": true,
    "types": ["node", "vitest/globals"]
  },
  "include": ["srv", "test", "@cds-models"]
}
